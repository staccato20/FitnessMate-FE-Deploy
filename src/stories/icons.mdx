import { IconGallery, IconItem, Meta, Title } from "@storybook/blocks"

import Icon from "../components/Icon/Icon.tsx"
import { icons } from "../components/Icon/utils/icons.ts"

<Meta title="Getting Started/Icon" />

# Icons

there are {icons.length} icons

<br />

<IconGallery style={{ marginTop: "50px" }}>
  {icons.map((key) => (
    <IconItem name={key}>
      <Icon icon={key} />
    </IconItem>
  ))}
</IconGallery>
