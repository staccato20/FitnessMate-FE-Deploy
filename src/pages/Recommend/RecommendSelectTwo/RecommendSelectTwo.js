import { RecommendContainer } from "../StyledRecommend";

// 버튼과 이미지의 간격을 어떻게 줄지 고민해 봐야함.
const RecommendSelectTwo = () => {
  return <RecommendContainer>gg</RecommendContainer>;
};

export default RecommendSelectTwo;
