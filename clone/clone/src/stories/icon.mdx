import { IconGallery, IconItem, Meta, Title } from "@storybook/blocks"

import { ICON_MAP } from "../components/Icon/Icon.tsx"
import Icon from "../components/Icon/Icon.tsx"

<Meta title="Getting Started/Icon" />

# Icons

there are {Object.keys(ICON_MAP).length} icons

<br />

<IconGallery style={{ marginTop: "5rem" }}>
  {Object.keys(ICON_MAP).map((key) => (
    <IconItem name={key}>
      <Icon icon={key} />
    </IconItem>
  ))}
</IconGallery>
